# Geneligence

### Локальный запуск проекта

Перед началом работы убедитесь, что ваша система готова к запуску проекта.

#### 🔍 Проверка предварительных требований

**1. Проверьте наличие Node.js:**
```bash
node -v
```
*Если команда не распознана, [установите Node.js](https://nodejs.org/)*

**2. Проверьте наличие pnpm:**
```bash
pnpm -v
```
*Если pnpm не установлен, выполните:*
```bash
npm install -g pnpm@latest
```

#### 🚀 Запуск проекта

**1. Установите зависимости:**
```bash
pnpm install
```

**2. Запустите сервер разработки:**
```bash
pnpm dev
```

Проект будет доступен по адресу:  
**http://localhost:3000**

#### 📋 Краткая инструкция для быстрого старта:

```bash
# Клонируйте проект (если нужно)
git clone <repository-url>

# Перейдите в директорию проекта
cd project-directory

# Установите зависимости
pnpm install

# Запустите разработку
pnpm dev
```

#### ⚡ Готово!
Теперь вы можете работать с проектом локально. Сервер разработки автоматически перезагружает страницу при изменениях в коде.