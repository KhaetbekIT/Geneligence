import type { PageType } from "@/types/type";
import { ProjectList } from "./_components/project-list";

const Page: PageType = () => {
	return (
		<>
			<ProjectList />
		</>
	);
};

export default Page;
